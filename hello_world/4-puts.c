#include <stdio.h>

/**
* main - fonction pricipale
* puts - commande affichant le message
* Return: retourner 0 si le code n'a pas d'erreur
*/

int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
